// Reusable card components for Liberty Ridge Training Grounds

export { default as CourseCard } from './CourseCard';
export { default as FeatureCard } from './FeatureCard';
export { default as TestimonialCard } from './TestimonialCard';
export { default as StatsCard } from './StatsCard';

// Additional card types that can be created as needed:
// - TeamMemberCard (for team/instructor bios)
// - BlogCard (for news/articles)
// - EventCard (for upcoming events/schedules)
// - LocationCard (for training locations)
// - CertificationCard (for certifications/badges)